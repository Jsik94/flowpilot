# FlowPilot Docker Compose 환경변수

# 포트
FRONTEND_PORT=8080
BACKEND_PORT=3001

# Backend CORS 허용 origin
FRONTEND_URL=http://localhost:8080

# Frontend → Backend API 경로 (Docker nginx 프록시 시 /api)
VITE_API_URL=/api

# Gemini AI (비워두면 휴리스틱 분석만 사용)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
